export interface InsurantInfo {
    registrationNumber: string;
    firstName: string;
    lastName: string;
    passportNumber: string;
    birthDate: string;
}

// export interface InsurantInfoList { id: number; info: InsurantInfo }[] = [];